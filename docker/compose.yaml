services:
  # ===============================
  # MongoDB
  # ===============================
  mongodb:
    image: mongo:7
    container_name: rag-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
      MONGO_INITDB_DATABASE: ragmongo
    volumes:
      - mongo_rag_data:/data/db
      - ../logs:/logs
    command: >
      sh -c "mongod --bind_ip_all
      2>&1 | tee /logs/mongodb.log"
    networks:
      - rag-network

  # ===============================
  # PostgreSQL + PGVector
  # ===============================
  postgres:
    image: pgvector/pgvector:pg16
    container_name: rag-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ragdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../logs:/logs
    command: >
      sh -c "docker-entrypoint.sh postgres
      2>&1 | tee /logs/postgres.log"
    networks:
      - rag-network

  # ===============================
  # Redis
  # ===============================
  redis:
    image: redis:7-alpine
    container_name: rag-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ../logs:/logs
    command: >
      sh -c "redis-server
      2>&1 | tee /logs/redis.log"
    networks:
      - rag-network

  # ===============================
  # Adminer
  # ===============================
  adminer:
    image: adminer
    container_name: rag-adminer
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    networks:
      - rag-network

  # ===============================
  # Redis UI
  # ===============================
  redis-ui:
    image: rediscommander/redis-commander:latest
    container_name: rag-redis-ui
    restart: always
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis
    networks:
      - rag-network

  # ===============================
  # NestJS Backend
  # ===============================
  backend:
    build:
      context: ../backend_nestjs
      dockerfile: ../docker/Dockerfile.backend.dev
    image: genai-langchain-learn-nestjs-backend:dev
    container_name: genai-langchain-learn-nestjs-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/ragdb
      REDIS_URL: redis://redis:6379
      MONGO_URL: mongodb://root:root@mongodb:27017/ragmongo?authSource=admin
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      VECTOR_STORE_PATH: /app/vector_store
      NODE_ENV: development
    depends_on:
      - postgres
      - redis
      - mongodb
    volumes:
      - ../backend_nestjs:/app
      - vector_data:/app/vector_store
      - ../logs:/logs
    command: >
      sh -c "npm run start:dev
      2>&1 | tee /logs/backend.log"
    networks:
      - rag-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ===============================
  # Next.js Frontend
  # ===============================
  app:
    build:
      context: ../frontend_nextjs
      dockerfile: ../docker/Dockerfile.fe.dev
    image: genai-langchain-learn-nestjs-frontend:dev
    container_name: genai-langchain-learn-nestjs-frontend
    restart: always
    ports:
      - "8082:3000"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:5000
    volumes:
      - ../frontend_nextjs:/app
      - ../logs:/logs
    command: >
      sh -c "npm run dev
      2>&1 | tee /logs/frontend.log"
    depends_on:
      - backend
    networks:
      - rag-network

# ===============================
# Volumes
# ===============================
volumes:
  postgres_data:
  vector_data:
  mongo_rag_data:

# ===============================
# Network
# ===============================
networks:
  rag-network:
    driver: bridge
